# Trading Buddy — pinned dependencies (Python 3.11+)
# Broker
kiteconnect==5.0.1
# LLM (needs >=0.50 for output_config structured outputs + 2026 model IDs)
anthropic==0.109.1
# News
feedparser==6.0.11
requests==2.32.3
# Data / indicators
pandas==2.2.3
numpy==2.1.3
# Telegram alerts (direct requests is also fine; bot lib used for commands)
python-telegram-bot==21.7
# Scheduling
APScheduler==3.10.4
# 2FA / TOTP
pyotp==2.9.0
# Web dashboard
fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart==0.0.20
# Config / secrets
python-dotenv==1.0.1
# Timezone (stdlib zoneinfo used; tzdata needed on Windows)
tzdata==2024.2
